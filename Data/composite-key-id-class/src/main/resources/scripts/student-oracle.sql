CREATE TABLE STUDENT (
	ROLL_NUMBER NUMBER(4),
	FIRST_NAME VARCHAR2(30),
	LAST_NAME VARCHAR2(30),
	AGE NUMBER(2),
	CONSTRAINT ROLL_NAME_PK PRIMARY KEY (ROLL_NUMBER, FIRST_NAME)
);

INSERT INTO STUDENT VALUES (1000, 'SUBODH', 'KUMAR', 30);
SELECT * FROM STUDENT;